import PPaginationSelect from './src'

/* istanbul ignore next */
PPaginationSelect.install = function (Vue) {
  Vue.component(PPaginationSelect.name, PPaginationSelect)
}

export default PPaginationSelect
